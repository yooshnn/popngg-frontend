/** The label that toggles a whole group, sitting in the grid's first column. */
export const groupToggleClassName = 'mt-0.5 -ml-1 h-7 justify-start rounded px-1 text-left font-mono text-[.6875rem] tracking-[.02em] text-fg-neutral-subtle hover:text-fg-neutral active:rounded';

/** One group member, sized so the checkboxes line up into columns. */
export const memberCheckboxClassName = 'w-13 min-w-0 shrink-0 gap-1.5 px-0 text-xs';
