{
  "name": "popngg-frontend",
  "private": true,
  "packageManager": "pnpm@11.14.0",
  "engines": {
    "node": "24.14.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "COMMIT_SHA=$(git rev-parse --short HEAD) turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.10.8"
  }
}
